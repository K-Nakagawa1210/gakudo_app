<!DOCTYPE html>
<html>
  <head>
    <title>出欠管理アプリ</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if user_signed_in? %>
      <nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm mb-3">
        <div class="d-flex">
          <%= link_to "トップに戻る", root_path, class: "btn btn-outline-primary btn-sm me-2" %>
        </div>
      </nav>
    <% end %>
    
    <div class="container flash-messages"> 
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
    </div>

    <%# ログイン/サインアップ画面にのみ適用するCSSコンテナの場所を定義 %>
    <% if controller_name == 'sessions' || controller_name == 'registrations' %>
      <%# Deviseフォーム専用のラッパー (中央配置を妨げない) %>
      <div class="devise-center-wrapper">
        <%= yield %>
      </div>
    <% else %>
      <%# その他のページはBootstrapのコンテナを使用 %>
      <div class="container">
        <%= yield %>
      </div>
    <% end %>
  </body>
</html>
