<div class="main-container text-center">

  <h1 class="mb-4">出席登録（学校一覧）</h1>

  <div class="back-button-area mb-4">
    <%= link_to "前のページに戻る", root_path, class: "btn btn-outline-secondary btn-sm" %>
  </div>

  <div class="card shadow-sm">
    <div class="card-header bg-primary text-white">
      登録する学校を選択してください
    </div>

    <div class="card-body">
      <% if @schools.any? %>
        <table class="table table-hover table-bordered align-middle"> 
          <tbody>
            <% @schools.each do |school| %>
              <tr>
                <td class="fw-bold">
                  <%= link_to school.school_name, school_students_path(school),
                      class: "text-decoration-none text-primary fw-bold" %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      <% else %>
        <p class="text-center text-muted">登録されている学校がありません。</p>
      <% end %>
    </div>
  </div>

</div>